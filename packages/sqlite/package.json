{
  "name": "@agent-control-plane/sqlite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "npm run build --workspace @agent-control-plane/core && tsc -p tsconfig.json",
    "check": "npm run build --workspace @agent-control-plane/core && tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node dist/self-test.js"
  },
  "dependencies": {
    "@agent-control-plane/core": "0.1.0"
  }
}
